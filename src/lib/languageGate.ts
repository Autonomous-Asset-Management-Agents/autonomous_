/**
 * Language gate (operator 14.08.): German stays OFF until the full main-page
 * translation ships — the tower page runs English-only and no language toggle
 * is rendered anywhere. Flip to true in the DE-launch PR.
 */
export const GERMAN_ENABLED = false;
