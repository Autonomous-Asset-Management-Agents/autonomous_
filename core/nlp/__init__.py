# core/nlp — RTR-1 (#1948): local, free NLP sentiment for the NewsSentimentAgent.
# Edition-neutral finance-core (BORA): reads config ONLY via config.get_config(),
# never os.environ (CODING_POLICY §2.10). No paid APIs (feedback_no_paid_apis).
